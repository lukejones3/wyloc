paths-ignore:
  # Pattern definitions and test fixtures intentionally contain format-matching
  # synthetic strings to exercise the detection engine's regexes. These are
  # fake/test values — no live credentials, sequential or repeating characters only.
  - 'packages/detector/src/patterns/definitions/**'
  - 'packages/detector/test/fixtures/**'
